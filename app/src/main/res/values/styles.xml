<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- res/values/styles.xml -->
        <!-- Định nghĩa kiểu chữ cho bottom menu -->
        <style name="BottomNavigationTextAppearance" parent="@android:style/TextAppearance">
            <item name="android:fontFamily">@font/roboto</item>
            <item name="android:textSize">12sp</item>
            <item name="android:textColor">@color/black</item>
        </style>

        <style name="BottomNavigationTextAppearanceSelected" parent="@android:style/TextAppearance">
            <item name="android:fontFamily">@font/roboto_bold</item>
            <item name="android:textSize">14sp</item>
            <item name="android:textColor">@color/black</item>
        </style>
    <style name="AppTheme.BottomNavigationView" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="itemBackground">@null</item>
        <item name="itemIconTint">@null</item>
    </style>
    <style name="CustomDatePickerDialog" parent="Theme.AppCompat.Light.Dialog">
        <!-- Nền dialog -->
        <item name="android:windowBackground">@color/white</item>
        <!-- Màu cho nút OK, Cancel -->
        <item name="android:buttonBarStyle">@style/CustomButtonBar</item>
    </style>

    <style name="CustomButtonBar" parent="Widget.AppCompat.ButtonBar">
        <item name="android:buttonStyle">@style/CustomButton</item>
    </style>

    <style name="CustomButton" parent="Widget.AppCompat.Button">
        <item name="android:textColor">@color/white</item>
    </style>
</resources>